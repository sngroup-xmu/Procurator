# Ultimate / GemCutter

This directory contains the pinned Ultimate/GemCutter source tree used by the
evaluation workflow. Public releases should state whether the AE package ships a
prebuilt `UGemCutter-linux/Ultimate` binary or builds it from source.

Procurator's own Ultimate toolchain and setting files remain under
`src/dslc/toolchain/ultimate/`.
