# Input Inference And Slicing

Input inference evidence should explain kept, havoced, pruned, forced-kept, and
skipped variables. Slicing seeds come from assertions, DSL property
dependencies, and conservative on-wire packet-field propagation across topology
links.
